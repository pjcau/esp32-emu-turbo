FROM kicad/kicad:10.0-full

# KiCad 10 (10.0-full) bundles kicad-packages3d — no apt install needed.
# Tag: 10.0-full tracks the latest KiCad 10.0.x stable release.

WORKDIR /project
ENTRYPOINT ["kicad-cli"]
