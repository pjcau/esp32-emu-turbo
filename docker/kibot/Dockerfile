FROM ghcr.io/inti-cmnb/kicad9_auto:latest

# KiBot external DFM analysis container
# Provides: KiBot 1.8.5 + KiCad 9 (DRC/ERC/design report)
# Platform: runs under emulation on arm64 (M1/M2 Mac)

WORKDIR /project
ENTRYPOINT ["kibot"]
